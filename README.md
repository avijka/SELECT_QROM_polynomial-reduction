# SELECT_QROM_polynomial-encoding
a SELECT-style quantum ROM for boolean functions in Qiskit; improves circuit complexity over the naive SELECT by producing a polynomial representation of the function
